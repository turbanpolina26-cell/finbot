{
  "name": "finbot-proxy",
  "version": "1.0.0",
  "description": "Proxy for Supabase requests (used to work around mobile/Telegram network restrictions)",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
